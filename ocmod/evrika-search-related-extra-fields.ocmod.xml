<?xml version="1.0" encoding="UTF-8"?>
<modification>
    <name>Evrika: Search and Related — extra product fields</name>
    <code>evrika-search-related-extra-fields</code>
    <version>1.0</version>
    <author>Evrika</author>
    <link></link>

    <!--
        Добавляет поля model, manufacturer, stock, has_required_options
        в массив $data['products'][] в:
          - catalog/controller/product/search.php
          - catalog/controller/product/product.php (блок related products)

        Нужны для корректного отображения карточек товара
        (наличие, код, производитель, кнопка «Купить сейчас»).
    -->

    <!-- ══════════════════════════════════════════════════
         search.php — href уникальна: содержит переменную $url
    ══════════════════════════════════════════════════ -->
    <file path="catalog/controller/product/search.php">
        <operation>
            <search><![CDATA['href'        => $this->url->link('product/product', 'product_id=' . $result['product_id'] . $url)]]></search>
            <add position="after"><![CDATA[					'model'                => $result['model'],
					'manufacturer'         => $result['manufacturer'],
					'stock'                => ($result['quantity'] > 0 || !$result['subtract']),
					'has_required_options' => (bool)$this->db->query("SELECT COUNT(*) AS cnt FROM `" . DB_PREFIX . "product_option` WHERE product_id = '" . (int)$result['product_id'] . "' AND required = '1'")->row['cnt'],]]></add>
        </operation>
    </file>

    <!-- ══════════════════════════════════════════════════
         product.php — href related-блока: без $url, уникальна в файле
    ══════════════════════════════════════════════════ -->
    <file path="catalog/controller/product/product.php">
        <operation>
            <search><![CDATA['href'        => $this->url->link('product/product', 'product_id=' . $result['product_id'])]]></search>
            <add position="after"><![CDATA[					'model'                => $result['model'],
					'manufacturer'         => $result['manufacturer'],
					'stock'                => ($result['quantity'] > 0 || !$result['subtract']),
					'has_required_options' => (bool)$this->db->query("SELECT COUNT(*) AS cnt FROM `" . DB_PREFIX . "product_option` WHERE product_id = '" . (int)$result['product_id'] . "' AND required = '1'")->row['cnt'],]]></add>
        </operation>
    </file>

</modification>
